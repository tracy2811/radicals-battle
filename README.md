# Radicals Battle

Realtime 2-player multiple-choice game in Express (NodeJS) using socket.io

## Run server

  * On Local Machine

    ```
    node dist/server.js
    ```

  * Using Docker

    ```
    docker-compose up
    ```
